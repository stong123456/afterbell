# 原型验收记录

## 中英文切换增量验证
语言切换覆盖导航、表单、场景说明、规则结果与导出 Markdown。浏览器实测：英文压力测试完整输出、刷新保留 en、Portfolio 英文转中文后数值不变、中文首页渲染正常；error 日志为空。6/6 测试通过（含原文与用户输入保留、英文报告生成内容无中文遗漏）。生产构建通过。真实来源文本保持原语言。

2026-09-14（北京时间）。构建：Vite production build 通过。Node 规则测试 4/4 通过；server.mjs 语法检查通过。/api/health、/api/news、/api/market 实际返回 200；StoneDaily 公共接口实际返回新闻来源与 Bitget rToken 数据。供应商状态随请求变化，不保证持续在线。

使用 Codex 内置浏览器，无 Playwright 外部浏览器回退。验证 1536×1024 桌面和 390×844 手机视口。手机 document.scrollWidth=375，小于视口 390，没有页面横向溢出。行情表内部允许滚动。

已走通：Radar → 演示情景 → Event → 输入交易观点 → Stress-test → 反证与缺失证据。Portfolio 10000/-5 得 -500 USD；Edge 98/100 得 -2.00%。浏览器 error 日志为空。报告导出按钮已实现，文件下载落盘尚未单独验收。

## 视觉对照
已用 view_image 对照 design/concept.png 与 design/desktop.png；截图来自内置浏览器。保留五处核心设计：248px 侧栏、衬线双行主标题、深蓝/黄绿色调、事件与 Challenge 双栏、底部资产表。移动端改为顶部五项导航与单列。

有意变化：界面说明采用中英混排；LIVE EVENTS 改为 EVENT RADAR，避免情景被误当实时新闻；增加来源/情景切换与规则模式标签；实时记录较长，列表内部滚动；报价已显示实际聚合数据。修复手机导航横向滚动、移动端来源入口缺失和行情时间标识。

首屏文字对照：主标题和副标题一致；上述来源、模式和中文说明为数据诚实性所需的有意增加。主体布局已按概念核对，但截图清晰度受内置浏览器捕获影响，不声称像素级 10/10 一致。当前为可运行产品原型，不是完整参赛成品。
